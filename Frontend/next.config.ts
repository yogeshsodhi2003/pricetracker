import type { NextConfig } from "next";

const nextConfig: NextConfig = {
   images: {
    domains: ['avatars.githubusercontent.com','images.unsplash.com', 'plus.unsplash.com','lh3.googleusercontent.com','webscraper.io'], // add your trusted domains here
  },
  /* config options here */
};

export default nextConfig;
